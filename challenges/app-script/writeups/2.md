## Challenge name: Bash - System 2


<img width="1333" height="328" alt="image" src="https://github.com/user-attachments/assets/85443ad8-45af-4127-b554-0fa7956b8341" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/App-Script/ELF32-System-1](https://www.root-me.org/en/Challenges/App-Script/ELF32-System-1?lang=en)
<br>
<br>

**Statement:** <br>
*Source code*
```

    #include <stdlib.h>
    #include <stdio.h>
    #include <unistd.h>
    #include <sys/types.h>
     
    int main(){
        setreuid(geteuid(), geteuid());
        system("ls -lA /challenge/app-script/ch12/.passwd");
        return 0;
    }

```

<br>
Challenge connection informations:

```
Host	challenge02.root-me.org
Protocol	SSH
Port	2222
SSH access 	ssh -p 2222 app-script-ch12@challenge02.root-me.org 
Username	app-script-ch12
Password	app-script-ch12
```

<br>

**After connecting to challenge using ssh we can see that there is .passwd file where our file is and binary ch12 have access to it.**

<br>
<img width="1501" height="390" alt="image" src="https://github.com/user-attachments/assets/b49a4fd5-cad6-4975-904a-a48c7f29784d" />
<br>
<br>

**The code of that binary is provided and in that c code we can see that its using system() command and system funciton uses $PATH variable and runs all runs binaires form command finding in $PATH whih is /usr/bin/**

<br>
<img width="1274" height="220" alt="image" src="https://github.com/user-attachments/assets/5a3f2968-b06c-4eee-8824-3ad5da818bc2" />
<br>
<br>

**So now if we change our path to /tmp and we make ls named binary or a bash executable without sh format and put cat inside to read .passwd file it will solve the challenge so lets begin.**

<br>
<img width="1274" height="180" alt="image" src="https://github.com/user-attachments/assets/273e0f5a-73f5-4a76-b584-04052514007a" />
<br>
<br>


**The challange makers were clever they blocked tmp so we can't use but ther is a folder called shm in dev which stores things in memory and will be vanished after a restart. In previous challenge we had access to it but now its blocked lets see where we can write a file..**


<br>
<img width="1274" height="529" alt="image" src="https://github.com/user-attachments/assets/b5fbfd7d-6d17-41ff-b002-5612b8ae9106" />
<br>
<br>

**After roaming I found a location at `cd /run/user/$(id -u)` where we have write access and now lets craft our payload as:**

<br><br>
```
#!/bin/sh  
cat /challenge/app-script/ch11/.passwd
```

<br>
<img width="1333" height="328" alt="image" src="https://github.com/user-attachments/assets/fcd47c0d-b7ee-46d5-affa-c910bfd8c564" />
<br>
<br>

**Our payload/new executable as replacement of `ls` is ready now lets change the $PATH var to `/dev/shm` using this command:** <br><br>
```
export PATH=/dev/shm:$PATH
```

<br>

**After that cd to users home ~ dir and run the binary using ./ch11 or bash ch11 whatever you prefer and ls will not work now in current tty cuz our custom one is there and it will run. Don't get confused and cd works because its not a binary in /bin/ its inbuild into shell maybe for faster execution I suspect so do cd ~ && ./ch11 and let the magic happen..**

<br>
<img width="1333" height="328" alt="image" src="https://github.com/user-attachments/assets/48d6a688-b2ca-47d8-baa2-f837200f30ab" />
<br>
<br>

**As you can see it printed the .passwd content. So, we can use this "!oPe96a/.s8d5" as flag/passwd to validate the challenge..** <br>
**Thankyou.**




